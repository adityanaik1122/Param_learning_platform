const fs = require('fs'); 
const p = 'frontend\\src\\features\\learning-path\\GameDevelopment_Content.tsx'; 
let s = fs.readFileSync(p, 'utf8');
s = s.replace(\"      '### Assessment Task', \n      ...m.assessment.map(function (a) { return '- ' + a; }) \", \"      '### Assessment Task', \n      ...m.assessment.map(function (a) { return '- ' + a; }), \n      '', \n      '### Chapter Exercise Pack', \n      '- Baseline: Extract and run `Chapter' + moduleNumber + 'Code.zip`.', \n      '- Task A: Document baseline behavior and script responsibilities.', \n      '- Task B: Implement one feature extension aligned to this module.', \n      '- Task C: Add reliability checks and run 8 manual tests.', \n      '', \n      '### Submission Artifacts', \n      '- `CHANGELOG_ch' + moduleNumber + '.md`', \n      '- `TESTLOG_ch' + moduleNumber + '.md`', \n      '- Updated project files and a short demo video' \");
const codePattern = /\s*code:\s*\[[\s\S]*?\]\.join\('\\n'\)\s*/; 
const codeReplacement = \"\n    code: [\n      '// Chapter ' + moduleNumber + ' practical starter',\n      '// Reference zip: Chapter' + moduleNumber + 'Code.zip',\n      'using UnityEngine;',\n      '',\n      'public class Chapter' + moduleNumber + 'Exercise : MonoBehaviour',\n      '{',\n      '    [Header(\\\\\\\"Module Context\\\\\\\")]',\n      '    [SerializeField] private string moduleTitle = \\\\\\\"' + m.moduleTitle + '\\\\\\\";',\n      '    [SerializeField] private string chapterZip = \\\\\\\"Chapter' + moduleNumber + 'Code.zip\\\\\\\";',\n      '',\n      '    [Header(\\\\\\\"Implementation Checklist\\\\\\\")]',\n      '    [SerializeField] private bool baselineRunComplete = false;',\n      '    [SerializeField] private bool extensionComplete = false;',\n      '    [SerializeField] private bool testsLogged = false;',\n      '',\n      '    private void Start()',\n      '    {',\n      '        Debug.Log(\\\\\\\"Module: \\\\\\\" + moduleTitle);',\n      '        Debug.Log(\\\\\\\"Load baseline from: \\\\\\\" + chapterZip);',\n      '        Debug.Log(\\\\\\\"Focus: ' + m.coreFocus + '\\\\\\\");',\n      '    }',\n      '',\n      '    // TODO 1: Reproduce baseline behavior from chapter code.',\n      '    // TODO 2: Implement one extension feature for this module.',\n      '    // TODO 3: Record 8 manual test cases in TESTLOG.',\n      '    // TODO 4: Summarize technical changes in CHANGELOG.',\n      '}',\n    ].join('\\n')\n\";
s = s.replace(codePattern, codeReplacement); 
fs.writeFileSync(p, s); 
console.log('patched gd content');
